import { Box } from "@mui/material";

export default function Nav() {
  return (
    <Box sx={{position: "fixed", top: '0', width: '100vw', height: '4rem',
      background: '#4285F4', zIndex: '333'}}>

    </Box>
  )
}
